using UnityEngine;

public class ShutdownComputerPayment : MonoBehaviour
{
    //[SerializeField] private ComputerPayment[] _computersPayment;

    //private void OnTriggerEnter(Collider other)
    //{
    //    if (other.gameObject.TryGetComponent(out ComputerPayment computerPament))
    //        SwitchComputersPayment(computerPament, false);
    //}

    //private void OnTriggerExit(Collider other)
    //{
    //    if (other.gameObject.TryGetComponent(out ComputerPayment computerPament))
    //        SwitchComputersPayment(computerPament, true);
    //}

    //private void SwitchComputersPayment(ComputerPayment computer, bool active)
    //{
    //    foreach (var computerPayment in _computersPayment)
    //    {
    //        if (computerPayment != computer)
    //            computerPayment.gameObject.SetActive(active);
    //    }
    //}
}
